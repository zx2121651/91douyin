package com.app.douyin.pro.feature.record.domain.usecase

import javax.inject.Inject

/**
 * 封装复杂的相机与录制控制逻辑
 * UI 仅需调用开始/停止/切换
 */
class CameraControlUseCase @Inject constructor() {
    // 逻辑待填充，目前作为架构存根
}
